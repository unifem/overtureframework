This directory holds regressions tests 

Type "make" to run the regression tests.

The file "tests" holds the list of tests to run.

To run one check:
  check.p shockTubeg

To replace the check file:
  check.p replace square.mp

To replace all check files
  check.p replace
